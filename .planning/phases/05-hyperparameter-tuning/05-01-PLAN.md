---
phase: 05-hyperparameter-tuning
status: default
wave: 1
depends_on: []
files_modified:
  - train_action_predictor_embedding.py
autonomous: true
---

# Phase 5: Hyperparameter Tuning - Execution Plan

## 1. Integrate Optuna Tuning Harness

<read_first>
- train_action_predictor_embedding.py
- .planning/phases/05-hyperparameter-tuning/05-CONTEXT.md
- .planning/phases/05-hyperparameter-tuning/05-RESEARCH.md
</read_first>

<acceptance_criteria>
- `train_action_predictor_embedding.py` successfully imports `optuna`.
- The CLI parser accepts `--tune` and `--n_trials` arguments.
- Running without `--tune` trains exactly like before (preserves standard execution).
- Running with `--tune` triggers the Optuna trial loop and saves runs to SQLite (`tuning_history.db`).
- The objective function prunes unpromising trials using an Optuna Keras callback.
</acceptance_criteria>

<action>
Modify `train_action_predictor_embedding.py` strictly as follows:

1. **Imports**: Add `import optuna` and `from optuna.integration import TFKerasPruningCallback` at the top of the file.
2. **Update CLI Parser**:
   - `parser.add_argument('--tune', action='store_true', help="Run Optuna hyperparameter tuning.")`
   - `parser.add_argument('--n_trials', type=int, default=50, help="Number of tuning trials.")`
   - Pass `tune=args.tune` and `n_trials=args.n_trials` to `train_embedding_model(...)`.
3. **Refactor `build_embedding_model`**:
   - Expose the hyperparameters in the signature: `dense1_units=512, dense2_units=256, dense3_units=128, drop1_rate=0.25, drop2_rate=0.25, drop3_rate=0.2, learning_rate=0.001`.
   - Apply these kwargs directly to the defining Keras layers. For example: `x = layers.Dense(dense1_units, use_bias=False, name='dense1')(x)`. Do this for all 3 Dense layers and Dropout layers.
   - Adjust the optimizer initialization to explicitly use the `learning_rate` parameter: `optimizer=Adam(learning_rate=learning_rate)`.
4. **Refactor `train_embedding_model` Engine**:
   - Update its signature to accept `tune=False` and `n_trials=50`.
   - Remove the old snippet `if learning_rate != 0.001: model.optimizer.learning_rate.assign(learning_rate)` (as this is now handled inside `build_embedding_model`).
   - Find the exact location right before `# --- Build Model ---`.
   - Add an `if tune:` block containing a nested `objective(trial)` function:
     - *Inside `objective`*: Clear the backend session `keras.backend.clear_session()`.
     - Define the search space:
       - `lr = trial.suggest_float("lr", 1e-4, 1e-2, log=True)`
       - `drop1 = trial.suggest_float("drop1", 0.1, 0.4)`
       - `drop2 = trial.suggest_float("drop2", 0.1, 0.4)`
       - `drop3 = trial.suggest_float("drop3", 0.1, 0.4)`
       - `dense1 = trial.suggest_categorical("dense1", [256, 512, 1024])`
       - `dense2 = trial.suggest_categorical("dense2", [128, 256, 512])`
       - `dense3 = trial.suggest_categorical("dense3", [64, 128, 256])`
     - Build the model: `model = build_embedding_model(..., dense1_units=dense1, dense2_units=dense2, dense3_units=dense3, drop1_rate=drop1, drop2_rate=drop2, drop3_rate=drop3, learning_rate=lr)`
     - Define tuning callbacks (EarlyStopping + ReduceLROnPlateau as normal), and append `TFKerasPruningCallback(trial, "val_loss")`.
     - Fit the model using `verbose=0` to reduce output spam.
     - Return the minimum `val_loss` using `float(min(history.history['val_loss']))`.
   - *Still inside `if tune:`*:
     - `study = optuna.create_study(direction="minimize", storage="sqlite:///tuning_history.db", study_name="tf_action_predictor", load_if_exists=True, pruner=optuna.pruners.MedianPruner(n_startup_trials=5, n_warmup_steps=3))`
     - `study.optimize(objective, n_trials=n_trials)`
     - Add `print("Best trial:"); print(study.best_trial)` and return `None, None`.
   - *In the `else:` block*: Retain the original, pre-existing `model = build_embedding_model(...)` and `history = model.fit(...)` execution path exactly as it operates today for non-tuning (using `verbose=2`).
</action>
